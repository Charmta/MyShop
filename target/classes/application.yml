server:
  port: 8009
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mytest?serverTimezone=Asia/Shanghai
    username: root
    password: 535363
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false
    mode: HTML5
mybatis-plus:
  mapper-locations: classpath:/mappers/*.xml
  type-aliases-package: com.hugong.MyShop.pojo
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
